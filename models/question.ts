export interface ExistingQuestion {
  questionId: number;
  eventId: number;
  questionText: string;
  questionType: string;
}
export interface DefaultQuestion {
  questionText: string;
  questionType: string;
  questionTypeName: string;
}
